import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.learning.login',
  appName: 'login-social',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
